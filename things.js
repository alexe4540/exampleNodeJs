module.exports.arrayCounter = function(array) {
  return `В массиве находится ${array.length} элементов!`;
};

module.exports.mutiply = function(x, y) {
  return `${x} * ${y} = ${x * y}`;
};

module.exports.someValue = 228;
